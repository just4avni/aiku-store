import Link from 'next/link';import Image from 'next/image';import {Lock,ShieldCheck,Download} from 'lucide-react';
export function ProductCard({product}:any){return <Link href={`/store/${product.slug}`} className="group overflow-hidden rounded-2xl border border-white/10 bg-white/[.025] transition hover:-translate-y-0.5 hover:border-aiku-accent/30"><div className="relative aspect-[4/3] bg-aiku-accent/5">{product.thumbnail_url?<Image src={product.thumbnail_url} alt={product.name} fill className="object-cover transition duration-500 group-hover:scale-[1.03]" sizes="(max-width:768px) 100vw,33vw"/>:<div className="flex h-full items-center justify-center text-aiku-accent/30"><Download size={34}/></div>}<span className="absolute right-3 top-3 inline-flex items-center gap-1 rounded-full border border-black/20 bg-black/70 px-2.5 py-1 text-[11px] font-semibold backdrop-blur">{product.access_type==='vvip'?<ShieldCheck size={12}/>:product.access_type==='vip'?<Lock size={12}/>:null}{product.access_type==='public'?'Normal':product.access_type.toUpperCase()}</span></div><div className="p-4"><div className="flex items-start justify-between gap-3"><h3 className="font-bold">{product.name}</h3><span className="shrink-0 text-xs text-zinc-500">v{product.version}</span></div><p className="mt-2 line-clamp-2 text-sm text-zinc-500">{product.description||'Digital file from Aiku Store.'}</p><div className="mt-4 flex items-center justify-between text-xs text-zinc-600"><span>{product.category?.name||'Uncategorized'}</span><span>{product.download_count||0} downloads</span></div></div></Link>}
