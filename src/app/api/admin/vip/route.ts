import {NextResponse} from 'next/server';import {createAdminClient} from '@/lib/supabase/admin';import {randomKey,requireAdmin,sha256} from '@/lib/server-auth';
export const runtime='nodejs';
export async function GET(req:Request){const a=await requireAdmin();if('error'in a)return a.error;const pid=new URL(req.url).searchParams.get('product_id');let q=createAdminClient().from('vip_keys').select('id,product_id,key_prefix,label,is_active,created_at').order('created_at',{ascending:false});if(pid)q=q.eq('product_id',pid);const {data,error}=await q;return error?NextResponse.json({error:error.message},{status:500}):NextResponse.json({keys:data||[]})}
export async function POST(req:Request){const a=await requireAdmin();if('error'in a)return a.error;const b=await req.json();const productId=String(b.product_id||'');let key=String(b.key||'').trim().toLowerCase();if(!productId)return NextResponse.json({error:'Product required'},{status:400});if(!key)key=randomKey('aiku',1);if(!/^aiku-[a-z0-9]{4}$/.test(key))return NextResponse.json({error:'VIP key must look like aiku-abc1'},{status:400});const db=createAdminClient();const {error}=await db.from('vip_keys').insert({product_id:productId,key_hash:await sha256(key),key_prefix:key,label:b.label||null,created_by:a.user.id});if(error)return NextResponse.json({error:error.message},{status:400});return NextResponse.json({key})}
export async function PATCH(req:Request){const a=await requireAdmin();if('error'in a)return a.error;const b=await req.json();const db=createAdminClient();const {error}=await db.from('vip_keys').update({is_active:Boolean(b.is_active),updated_at:new Date().toISOString()}).eq('id',b.id);return error?NextResponse.json({error:error.message},{status:400}):NextResponse.json({ok:true})}
