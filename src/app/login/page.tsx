'use client';import{useState}from'react';import{createClient}from'@/lib/supabase/client';import{ShieldCheck,Loader2}from'lucide-react';export default function Login(){const[e,setE]=useState('');const[p,setP]=useState('');const[b,setB]=useState(false);const s=createClient();async function go(x:any){x.preventDefault();setB(true);const{error}=await s.auth.signInWithPassword({email:e,password:p});if(error)alert(error.message);else location.href='/admin';setB(false)}return <main className="flex min-h-screen items-center justify-center bg-aiku-bg p-5"><form onSubmit={go} className="w-full max-w-md rounded-2xl border border-white/10 bg-white/[.025] p-7"><div className="mb-7 text-center"><ShieldCheck className="mx-auto mb-4 text-aiku-accent" size={38}/><h1 className="text-2xl font-black">Aiku Admin</h1><p className="mt-1 text-sm text-zinc-500">Authorized administrators only.</p></div><input required type="email" placeholder="Email" value={e} onChange={x=>setE(x.target.value)} className="mb-3 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-aiku-accent/50"/><input required type="password" placeholder="Password" value={p} onChange={x=>setP(x.target.value)} className="mb-5 w-full rounded-xl border border-white/10 bg-black/20 px-4 py-3 outline-none focus:border-aiku-accent/50"/><button disabled={b} className="flex w-full items-center justify-center gap-2 rounded-xl bg-aiku-accent py-3 font-bold text-black">{b&&<Loader2 size={17} className="animate-spin"/>}Sign in</button></form></main>}
